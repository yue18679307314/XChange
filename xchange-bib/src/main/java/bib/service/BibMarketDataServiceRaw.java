package bib.service;

import org.knowm.xchange.Exchange;

public class BibMarketDataServiceRaw extends BibBaseService{
    /**
     * Constructor
     *
     * @param exchange
     */
    protected BibMarketDataServiceRaw(Exchange exchange) {
        super(exchange);
    }
}
