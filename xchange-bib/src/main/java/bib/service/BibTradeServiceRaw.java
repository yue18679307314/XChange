package bib.service;

import org.knowm.xchange.Exchange;

public class BibTradeServiceRaw extends BibBaseService{
    /**
     * Constructor
     *
     * @param exchange
     */
    protected BibTradeServiceRaw(Exchange exchange) {
        super(exchange);
    }
}
